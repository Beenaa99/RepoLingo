export interface FileData {
    name: string;
    path: string;
    type: 'file' | 'directory';
    children?: FileData[];
    content?: string;
}
export declare function scrapeRepositoryToJson(repoUrl: string): Promise<FileData[]>;
export declare function scrapeRepositoryToPlainText(repoUrl: string): Promise<string>;
